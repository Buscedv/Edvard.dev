# updates & installs
apt-get update
apt-get upgrade
apt-get autoremove
apt-get install nginx

# user setup
adduser git
usermod -aG sudo git
rsync --archive --chown=git:git ~/.ssh /home/git
echo "change to user git"
